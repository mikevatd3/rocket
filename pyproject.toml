[project]
name = "rocket"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "geoalchemy2>=0.20.0",
    "geopandas>=1.1.3",
    "owner-classification",
    "pandas>=3.0.3",
    "psycopg>=3.3.4",
    "sqlalchemy>=2.0.50",
]

[tool.uv.sources]
owner-classification = { path = "../../2_responsibilities/owner_classification", editable = true }
